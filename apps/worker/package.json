{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bullmq": "^5.5.0",
    "@assessment-ai/logger": "workspace:*",
    "@assessment-ai/queue": "workspace:*",
    "@assessment-ai/ai": "workspace:*",
    "puppeteer": "^22.6.0",
    "handlebars": "^4.7.8",
    "@assessment-ai/config": "workspace:*"
  },
  "devDependencies": {
    "@assessment-ai/tsconfig": "workspace:*",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.3"
  }
}
