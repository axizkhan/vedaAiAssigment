"use client";

import * as React from "react";
import { AnimatePresence, AnimatePresenceProps } from "framer-motion";

export const AnimatedPresence = (props: AnimatePresenceProps) => {
  return <AnimatePresence {...props} />;
};
